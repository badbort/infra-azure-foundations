name: SubscriptionFoundations
description: Manages resources groups, user assigned identities and role assignments scoped to the subscription and resource groups.
runtime: dotnet
config:
  pulumi:tags:
    value:
      pulumi:template: azure-csharp
